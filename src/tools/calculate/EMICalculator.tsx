import { useMemo, useState } from "react";
import { OutputPanel } from "../../components/common/OutputPanel";

export function EMICalculator() { const [principal, setPrincipal] = useState(500000); const [rate, setRate] = useState(10); const [months, setMonths] = useState(60); const emi = useMemo(() => { const r = rate / 12 / 100; return principal * r * ((1 + r) ** months) / (((1 + r) ** months) - 1); }, [months, principal, rate]); return <div className="tool-layout"><section className="tool-panel neu-card"><h2>Loan / EMI</h2><div className="field-grid"><label className="field">Loan amount<input className="tool-input" type="number" value={principal} onChange={(e) => setPrincipal(Number(e.target.value))} /></label><label className="field">Annual rate %<input className="tool-input" type="number" value={rate} onChange={(e) => setRate(Number(e.target.value))} /></label><label className="field">Months<input className="tool-input" type="number" value={months} onChange={(e) => setMonths(Number(e.target.value))} /></label></div></section><OutputPanel title="Monthly EMI"><div className="stat"><strong>{emi.toFixed(2)}</strong><span>per month</span></div></OutputPanel></div>; }
